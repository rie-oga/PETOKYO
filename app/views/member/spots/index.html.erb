<div class="container">
	<div class="row">
		あいうえお
		<%= @spots.each do |spot| %>
			<%= attachment_image_tag spot, :image, :fill, 500, 500, fallback: "no_image.jpg", size:'500x500' %>
			<%= spot.name %>
			<%= spot.address %>
			<%= spot.url %>
			<%= spot.introduction %>
		<% end %>
	</div>
</div>
